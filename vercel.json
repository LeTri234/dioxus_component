{
  "buildCommand": "curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y && source $HOME/.cargo/env && rustup target add wasm32-unknown-unknown && cargo install dioxus-cli && npm install && dx build --example demo --release --features web",
  "outputDirectory": "target/dx/demo/release/web/public",
  "framework": null
}
